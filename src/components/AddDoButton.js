export default function AddDoButton(props) {
    return (
        <div
        className="cb bg-blue"
        onClick={props.handleAddDoButtonClick}>
        <span>ADD DO</span>
        </div>
    )
}
